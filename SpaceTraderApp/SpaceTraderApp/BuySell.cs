using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp1
{
    class BuySell
    {
         public GameData Economy(GameData gameData)
        {
            bool buy = false;
            bool sell = false;
            var currency = gameData.Currency;
            if (buy == true)
            {
                

            }
        }
    }
}
